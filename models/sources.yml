version : 2
sources:
  - name: raw
    database : FINANCIAL_DWH
    schema: RAW
    tables:
      - name: RAW_ADVISORS
        config:
          loaded_at_field: LOAD_DATE
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour
      - name: RAW_ACCOUNTS
        config:
          loaded_at_field: LOAD_DATE
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour
      - name: RAW_CUSTOMERS
        config:
          loaded_at_field: LOAD_DATE
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour
      - name: RAW_SECURITIES
        config:
          loaded_at_field: LOAD_DATE
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour
      - name: RAW_TRANSACTIONS
        config:
          loaded_at_field: LOAD_DATE
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour